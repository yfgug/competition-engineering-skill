---
name: competition-engineering
description: 管理持续多轮、带量化评测、需要跨会话保存实验状态的竞赛工程与实验型论文研究。用于项目冷启动或存量接管、实验路线、基线与评测、claim-evidence 追踪、外部提交和可复现交付；不用于纯文案写作、普通文献综述、一次性 benchmark 或非实验型代码修改。
---

# 竞赛与实验研究工程化

目标：让人和 AI 在数周竞赛或论文实验中从可核验状态继续工作，减少上下文丢失、重复实验、评测噪声误判、基线污染、结论漂移和越权提交。

## 权威与权限

事实依据按以下顺序判断：

1. 官方规则、Q&A、官方评测结果，或论文研究中的原始数据、实验协议和一手来源。
2. 不可变原始日志、评测产物、数据快照及哈希。
3. Git commit、tag、diff 和可复现配置。
4. 实验笔记与索引。
5. 聊天中的回忆或口述结论。

笔记是决策与证据的持久化入口，不替代原始证据。口述结论可先写入笔记，但在证据不足时必须标为 `unverified`。

遵守宿主 Agent 和仓库已有的授权规则。不得从本 Skill 推断额外权限：

- push、上传、官方评测提交、论文投稿、发布和对外交付必须有用户明确授权。
- 本地 commit/tag 仅在用户明确要求或仓库规则已授权时执行。
- 不覆盖用户已有改动，不把无关改动纳入实验或提交。

## 条件阻断

- IF 开始一条新实验路线 -> 先读 `00_先看这里.md`、`AGENTS.md`、`notes/_closed_routes.md` 和相关评测记录。
- IF 路线已关闭且重试条件未满足 -> 停止该路线，说明证据和重试条件；不要把相近但实质不同的假设误判为同一路线。
- IF 开始修改源码 -> 记录版本控制预检：Git 仓库记录 `base_commit` 和初始 `git status --short`；非 Git 项目记录文件清单或哈希快照。
- IF 初始工作树存在来源不明或与目标文件重叠的改动 -> 停止并请用户确认。无关改动保留并绕开；本次实验产生的已知脏文件允许继续。
- IF 评测失败、环境异常或证据不足 -> 标记 `invalid` 或 `inconclusive`，先修复验证条件，不得直接判定路线失败。
- IF 官方出分与预期不符 -> 查本地评测的噪声模型和官方提交台账；不能仅凭重复官方提交估计方差，也不能仅凭“在波动范围内”禁止所有后续调查。
- IF 论文结论无法追到代码版本、数据版本、评测记录和图表生成过程 -> 标为 `unsupported` 或 `inconclusive`，不得写成已证实结论。
- IF 项目内文档使用机器绑定的绝对路径 -> 优先改为项目相对路径；确需绝对路径时同时记录环境标识和可迁移替代路径。

## 资源导航

| 文件 | 何时读 |
|---|---|
| `references/methodology.md` | 需要完整工程协议、路线状态机和版本管理细节时 |
| `references/evaluation.md` | 定义指标方向、约束、方差、A/B 评测和晋升规则时 |
| `references/research.md` | 做论文实验、消融、claim-evidence 矩阵、图表和复现材料时 |
| `references/adoption.md` | 接管已有且历史复杂的工程，或整理入口、旧笔记和重复目录时 |
| `references/templates.md` | 填写入口、笔记、台账和结果字段时 |
| `references/adaptations.md` | 按算法、系统、黑客松、CTF、机器人或科研赛种裁剪时 |
| `assets/scaffold/` | 创建项目骨架或读取规范模板时 |
| `scripts/scaffold.cjs` | 创建竞赛或研究骨架时；从本 Skill 目录解析绝对路径后执行 |
| `scripts/audit_workspace.cjs` | 只读审计现有工作区的结构、入口、路径、编码和交接就绪度时 |

## 冷启动

1. 先预览骨架：竞赛用 `node <skill-dir>/scripts/scaffold.cjs <目标目录> --dry-run`；论文研究增加 `--profile research`。确认后去掉 `--dry-run`。不要假设当前工作目录就是 Skill 目录。
2. 阅读官方规则与 Q&A，把限制翻译成可执行红线写入 `AGENTS.md`。`AGENTS.md` 只保存稳定规则，不维护动态权威笔记列表。
3. 在 `00_先看这里.md` 定义指标契约：主指标、`maximize|minimize`、硬约束、评测命令、噪声来源和晋升规则。
4. 跑通 baseline，记录 commit 或快照哈希、环境和原始结果。Git 未初始化时不要直接执行 tag；先说明并按用户选择初始化 Git 或使用哈希快照。
5. 在 `notes/_evaluations.md` 记录本地重复评测；`notes/_submissions.md` 只记录官方评测、论文投稿或 artifact 发布等真实外部提交，不为标定方差盲目消耗提交额度。
6. 写第一篇 open 笔记，并在 `00_先看这里.md` 留下当前阶段、推荐版本、权威证据和下一步唯一动作。

## 存量项目接管

不要对成熟项目直接运行 `--force` 或先搬目录。按 `references/adoption.md`：

1. 运行 `node <skill-dir>/scripts/audit_workspace.cjs <项目目录>` 做只读盘点。
2. 找出真实源码根、当前推荐版本、最新权威证据和用户已有改动。
3. 先创建或修复唯一动态入口，再把旧目录映射到规范职责；路径迁移前保留兼容说明。
4. 只在用户授权后移动、归档或删除历史材料。迁移本身写一篇笔记并记录未变清单。
5. 大型工作区全量扫描时用重复的 `--exclude <glob>` 排除复制仓库、vendor 和生成树；需要确认当前交接材料已填完时运行 `--ready --strict`，存量入口缺少项目类型时显式补 `--profile competition|research`。

## 会话协议

开始：读 `00_先看这里.md` -> 读 `AGENTS.md` -> 按入口指向读取最少必要的笔记和证据 -> 复述当前目标、基线、边界与本次动作。

结束：实质事件写笔记和证据路径 -> 必要时更新台账 -> 更新 `00_先看这里.md`。`notes/README.md` 只做历史索引，不复制当前状态。

## 实验循环

先判断工作类型：

- **因果/性能实验**：需要判断某个变量是否带来收益。优先单变量；组合实验单独评测。
- **修复/集成/基础设施工作**：允许一组完整且必要的改动，但必须记录文件范围、验证命令和未涉及的区域。

对可量化实验使用以下流程：

```text
定义假设与指标契约 -> 记录基线和初始状态 -> 写门槛
-> L0 冒烟/正确性 -> L1 同环境对照 -> L2 代表性或全量评测
-> L3 对照方向、约束、噪声余量和成本
-> pass: candidate / fail: rejected / 证据不足: inconclusive / 评测无效: invalid
-> 写笔记、台账和入口状态
```

规则：

- 门槛必须包含指标方向和硬约束；不是所有比赛都以“数值更高”为优。
- 单变量用于因果归因，不强制用于 bug 修复、必要的跨文件改动或不可拆分的集成。
- 未过门槛前先确认评测有效；`invalid` 和 `inconclusive` 不登记为已关闭路线。
- `rejected` 路线写清证据和重试条件，并登记 `notes/_closed_routes.md`。
- 组合收益不等于成员收益之和；组合候选必须重新走自己的 L1-L3。

## 论文研究模式

论文不是把比赛分数写成长文。读取 `references/research.md`，并遵守：

- 区分复现、探索性研究、确认性实验和竞赛结果转论文；不同类型使用不同强度的统计与结论措辞。
- 在 `paper/CLAIMS.md` 为每个核心 claim 记录适用范围、反证条件、实验 ID、代码/数据版本、统计依据和图表。
- 所有运行都进入评测台账；不得只保留最好结果。改变指标、数据切分、排除规则或门槛时记录时间和原因。
- 图表与表格必须能从版本化脚本和原始结果重建；手工编辑只允许展示层调整，不得改变数据含义。
- 引用必须核对原始来源；竞赛排行榜只证明锁定规则与环境下的结果，不能自动推出跨模型、跨硬件或普遍机制结论。
- 涉及人类数据、个人信息、许可、双重用途或研究伦理时，先满足相应审批和发布边界。

## 笔记与状态

笔记命名：`notes/YYYYMMDD_NN_主题_状态.md`，其中文件状态为 `open | pending | closed`。

实验结果使用独立字段：`pending | pass | fail | inconclusive | invalid`。路线生命周期为：

```text
proposed -> running -> candidate -> promoted
                    -> rejected
                    -> inconclusive
```

笔记头部和正文以 `assets/scaffold/notes/_TEMPLATE.md` 为唯一模板。历史笔记默认追加更正而不是重写；发现凭证、个人信息或危险错误时允许审计后修订或删除敏感内容，并留下更正记录。

## 晋升与保底

候选只有同时满足以下条件才可晋升：

1. 主指标按声明方向改善，并超过预先定义的噪声或最小实际收益门槛。
2. 所有硬约束通过；正确性、成本、稳定性等次级指标没有不可接受退化。
3. 结果可由记录的命令、环境和原始产物复现。
4. 外部验证是赛制或论文主张的必要条件时，已有用户授权且结果通过。

晋升后以 commit SHA 或内容哈希作为不可变身份；tag、压缩包和 SHA256 是可选的发布与灾备层。更新 `00_先看这里.md`、相关笔记和台账，后续实验从新保底开始。

## 交付

交付前执行允许清单式审计：版本与笔记一致、构建或运行可复现、manifest 与哈希可验证、无凭证和不必要的内部材料、在独立目录解压或部署成功。论文交付还要验证 claim-evidence、图表重建、数据许可、引用和匿名化。不要仅按文件类型粗暴删除 notebook、日志或其他可能属于复现证据的材料。

## 回复收口

推进冷启动、实验、晋升或交付时，在回复结尾给出与 `00_先看这里.md` 一致的“下一步唯一动作”。探索和复盘不强制；形成执行结论后再落盘。

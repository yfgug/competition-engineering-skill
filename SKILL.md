---
name: competition-engineering
description: 比赛工程化管理与 AI 协作持久化体系 (Competition engineering & AI-collaboration persistence)。适用于任何周期数周、有评测/评分的竞赛（算法/数据、系统性能、应用开发、安全/CTF、机器人、科研复现）。触发场景：开新比赛搭工程骨架、写/更新持久化实验笔记、定义量化门槛与验证流水线、固化合规红线、管理基线版本与不可污染保底、实验路线管理、交付打包。当用户提到"新比赛/开赛/搭目录/实验记录/路线关闭/保底版本/提交前验证/new competition/scaffold/experiment log/baseline"等时使用。
---

# 比赛工程化管理（AI 协作 + 持久化体系）

最高行动准则：你是竞赛工程的执行者与审计者，**拒绝一切口头约定，强制一切状态落盘**。笔记是唯一事实源；本文件未授权的"有外部后果的动作"（commit/push/提交/交付）一律先请示用户。

核心思想：把"人和 AI 一起做工程"中所有不可靠的部分（记忆、口头约定、临时判断）全部换成落盘协议（笔记、红线、门槛、快照），让每次会话、每个实验都从确定状态出发。

## 阻断式校验（if-then，命中即停）

- IF 用户要求开新路线 → 先查 `notes/_closed_routes.md` 与 `notes/_submissions.md`（方差）；命中"已关闭且重试条件未满足"→ 拒绝执行并说明依据。
- IF 即将修改比赛源码 → 先在目标工作区实际运行 `git status --short`（或要求用户贴结果）；不为空 → 停止，报告脏文件，禁止继续。
- IF 用户要求提交/推送/交付 → 无用户明确授权记录 → 停止请示。
- IF 官方出分与预期不符 → 先查 `_submissions.md` 方差台账；差异在波动范围内 → 判为噪声，禁止为噪声改代码。
- IF 用户口头给出实验结论但笔记中无记录 → 视为未验证，先补笔记再推进。

## 资源导航

| 文件 | 何时读 |
|---|---|
| `references/methodology.md` | 需要完整方法论细节时（七原则、路线生命周期、笔记七要素、验证流水线、交付管理） |
| `references/templates.md` | 需要抄模板时（入口文件、AGENTS.md 五段、笔记头部、closed 路线登记表、评测台账、执行卡、交付审计清单） |
| `references/adaptations.md` | 按赛种裁剪时（算法/系统/黑客松/CTF/机器人/科研 各自的替换与门槛示例） |
| `assets/scaffold/` | 骨架模板文件（被 scaffold 脚本复制，也可手动取用） |
| `scripts/scaffold.cjs` | 一键搭骨架 |

## 工作流 A：新比赛冷启动（Day 1–2）

1. 搭骨架：`node scripts/scaffold.cjs <目标目录>`（幂等，已有文件不覆盖）。
2. 通读官方规则/Q&A，把每条规则翻译成**可执行的红线**写入 `AGENTS.md` 第 2 段（"不改 X"格式，不是复述原文）。
3. 跑通 baseline → 打 tag / 压缩包 + SHA256 进 `backups/`，写第一篇笔记。
4. 方差标定：同一 baseline 评测 2–3 次，把波动范围记入笔记（此后小于波动的差异 = 噪声）。
5. 定第一轮路线的量化门槛（写在 open 笔记里，动手前定）。
6. 填写 `00_先看这里.md` 第一版。

## 工作流 B：每次 AI 会话的仪式

开始：读记忆 → 读 `00_先看这里.md` → 按 `AGENTS.md` 权威路由读 2–5 篇笔记 → 复述当前状态与本次边界再动手。
结束：实质事件先写笔记 → 更新 `00_先看这里.md` 与 `notes/README.md` 索引 → 留下"下一步唯一动作"。

环境纪律：

- 给出的任何命令必须声明执行环境（宿主机 PowerShell / WSL / 容器内 / 远程节点），不确定就先问；跨环境路径不一致时先确认再执行。
- 跨环境传输文本/源码警惕编码坑：不经二进制安全通道的复制可能引入编码损坏（UTF-16/GBK 乱码）；源码恢复用 git，不用剪贴板。

## 工作流 C：实验循环（单变量 + 门槛制）

```text
读权威笔记 → 定门槛(open笔记) → 单变量改动 → L0快检(只判能否继续)
→ L1同环境A/B(或A-B-A) → L2全量评测+有效性双保险 → L3对照门槛
→ 过线: 与保底对比, 用户授权后提交 / 不过线: 立即closed, 写重试条件
→ 写笔记 → 更新索引
```

铁律：

- 一次只改一个变量；一场会话只推进一个方向。
- 组合实验必须先有单变量结论，组合后**重新走 L1–L3 单独审计**（组合收益 ≠ 成员之和）。
- 门槛不过就关闭路线，不修修补补拖时间；closed 笔记必填"重试条件"，并同步登记 `notes/_closed_routes.md`。
- 实验只从干净基线副本做（动手前 `git status --short` 必须为空）。
- 保底（官方验证过的最优版本）不可污染、不可覆盖；提交/推送/交付需用户明确授权。
- 笔记必含"未变清单"（未 commit / 未 push / 未提交 / 未覆盖基线）——防自欺。

## 工作流 D：写持久化笔记（实质事件后必写）

触发：完整实验/官方出分/路线关闭/崩溃异常/提交交付/发现新坑/纠正旧结论。

命名：`notes/YYYYMMDD_NN_主题_状态.md`（open / pending / closed）。
头部五字段（状态、日期、supersedes、结论、重试条件）+ 正文七要素，完整模板见 `references/templates.md`。

状态生命周期：`open`（进行中）→ `pending`（等待全量/官方验证）→ `closed`（不过门槛）或晋升为保底。closed 必须同步登记 `notes/_closed_routes.md` 一行；官方提交/出分必须登记 `notes/_submissions.md` 一行。两条台账是防"重复烧算力"和"被方差欺骗"的最后防线。

索引纪律：`notes/README.md` 只做 authority 排序 + 一句话状态 + 文件列表；**摘要写在笔记里，不复制进索引**（索引复述正文必然失同步）。新笔记 supersede 旧笔记时，旧笔记不删不改。

## 工作流 E：脚本与产物命名

`<动词>_<对象>_<变体>_<日期>[_<环境标识>]`；动词固定语义：run/launch/poll/probe/inspect/check/setup/build/start/archive/analyze。结果归档 `results/<主题>_<日期>/summary.md`。

## 工作流 F：交付打包

单一 ZIP + SHA256 + manifest + 独立解压校验；交付前审计：无凭证泄漏（`.git/config`、token、AK/SK、笔记本、日志）、无脏文件、版本与笔记一致、一条命令可复现。`deliver/` 只留当前推荐版，旧版进 `archive/`。
**外部材料安全**：组委会/队友提供的任何 ZIP/源码，入库存档前先扫描凭证（`.git/config`、token 文件），有则记录并隔离处理。

## 工作流 G：路线晋升与基线更新（官方出分后）

```text
candidate 官方出分 → 与当前保底对比：
  高于保底 → 晋升为新的保底：
    1. git tag + zip + SHA256 进 backups/（基线三件套）
    2. 写 promotion 笔记（commit/成绩/对比数字），supersede 旧 best 笔记
    3. 更新 00_先看这里.md 的"当前最优版本"
    4. 评测台账 _submissions.md 追加一行
    5. 之后的实验从新保底拉分支
  不高于保底 → closed（记"重试条件"），保底不动，回滚到保底继续下一条路线
```

出分与预期不符时先查方差台账：差异在已标定波动范围内 → 视为噪声，禁止为噪声修改代码。

## 按赛种裁剪

四件套任何赛种不裁：**笔记系统、红线、基线不可污染、防自欺**。其余按 `references/adaptations.md` 的裁剪表替换实例化。

## 回复收口

推进工作流期间（冷启动/实验/晋升/交付），每次回复结尾输出一个**"下一步唯一动作"**——与 `00_先看这里.md` 的当前状态一致，并在实质事件后回写更新该文件。探索/复盘类讨论不受此限，但结论落地时同样只保留一个推荐动作。

# 已拒绝路线登记表

> 只登记评测有效且结果为 `fail` 的路线。`invalid` 和 `inconclusive` 不进入本表。

| 日期 | 路线 ID | 假设 | 指标契约 | 关键证据 | 重试条件 |
|---|---|---|---|---|---|
| YYYY-MM-DD | <route-slug> | <一句话> | <方向/约束/门槛> | <笔记或 results 路径> | <出现什么新证据才允许重开> |

重开前确认重试条件已经满足，并在新笔记中引用原路线证据。
